pipeline {
    agent{label 'slave'}
    stages {
        stage('Build and Push') {
            steps {
                echo 'Hello World'
                echo 'docker push'
            }
        }  
        stage('Deployment'){
            steps{
                echo "deployment"
            }
        }
        }
    }
